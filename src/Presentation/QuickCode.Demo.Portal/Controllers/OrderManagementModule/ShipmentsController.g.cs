// <auto-generated>
// QuickCode — DO NOT EDIT
// This file is regenerated by QuickCode. Edit DBML/DSL/templates or add user .cs files instead.
// Where to put custom code: see AGENTS.md at repo root.
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Linq;
using QuickCode.Demo.Portal.Models;
using QuickCode.Demo.Portal.Models.OrderManagementModule;
using QuickCode.Demo.Portal.Helpers;
using QuickCode.Demo.Infrastructure.Integration.Models;
using Microsoft.AspNetCore.Mvc;
using IdentityModuleContracts = QuickCode.Demo.Infrastructure.Integration.Nswag.Clients.IdentityModuleApi.Contracts;
using OrderManagementModuleContracts = QuickCode.Demo.Infrastructure.Integration.Nswag.Clients.OrderManagementModuleApi.Contracts;
using System.IO;
using Microsoft.AspNetCore.Authorization;
using QuickCode.Demo.Portal.Helpers.Authorization;
using Microsoft.AspNetCore.Http;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.Caching.Memory;
using Microsoft.AspNetCore.Mvc.Rendering;

namespace QuickCode.Demo.Portal.Controllers.OrderManagementModule
{
    [Permission("OrderManagementModuleShipments")]
    [Area("OrderManagementModule")]
    [Route("OrderManagementModuleShipments")]
    public partial class ShipmentsController : BaseController
    {
        private readonly OrderManagementModuleContracts.IShipmentsClient pageClient;
        private readonly OrderManagementModuleContracts.IOrdersClient pageOrderClient;
        private readonly OrderManagementModuleContracts.IShippingMethodsClient pageShippingMethodClient;
        public ShipmentsController(OrderManagementModuleContracts.IShipmentsClient pageClient, OrderManagementModuleContracts.IOrdersClient pageOrderClient, OrderManagementModuleContracts.IShippingMethodsClient pageShippingMethodClient, IdentityModuleContracts.ITableComboboxSettingsClient tableComboboxSettingsClient, IHttpContextAccessor httpContextAccessor, IMemoryCache cache) : base(tableComboboxSettingsClient, httpContextAccessor, cache)
        {
            this.pageClient = pageClient;
            this.pageOrderClient = pageOrderClient;
            this.pageShippingMethodClient = pageShippingMethodClient;
        }

        [ResponseCache(VaryByQueryKeys = new[] { "ic" }, Duration = 30)]
        public async Task<IActionResult> GetImage(string ic)
        {
            return await GetImageResult(pageClient, ic);
        }

        [Route("List")]
        public async Task<IActionResult> List()
        {
            var model = await PrepareModel();
            model.ComboList = await FillPageComboBoxes(model.ComboList);
            SetModelBinder(ref model);
            return View("List", model);
        }

        [Route("List")]
        [HttpPost]
        public async Task<IActionResult> List(ShipmentData model)
        {
            ModelBinder(ref model);
            model = await PrepareModel(model);
            model.SelectedItem = new();
            SetModelBinder(ref model);
            return View("List", model);
        }

        [Route("ListPartial")]
        [HttpPost]
        public async Task<IActionResult> ListPartial(ShipmentData model)
        {
            ModelBinder(ref model);
            model = await PrepareModel(model);
            model.SelectedItem = new();
            SetModelBinder(ref model);
            return PartialView("_ListTable", model);
        }

        [Route("Insert")]
        [HttpPost]
        public async Task<IActionResult> Insert(ShipmentData model)
        {
            ModelBinder(ref model);
            var result = await pageClient.ShipmentsInsertAsync(model.SelectedItem);
            ClearCache();
            SetModelBinder(ref model);
            return Ok(result);
        }

        [Route("Update")]
        [HttpPost]
        public async Task<IActionResult> Update(ShipmentData model)
        {
            ModelBinder(ref model);
            var request = model.SelectedItem;
            var result = await pageClient.ShipmentsUpdateAsync(request.Id, request);
            ClearCache();
            SetModelBinder(ref model);
            return Ok(result);
        }

        [Route("Delete")]
        [HttpPost]
        public async Task<IActionResult> Delete(ShipmentData model)
        {
            ModelBinder(ref model);
            var request = model.SelectedItem;
            var result = await pageClient.ShipmentsDeleteAsync(request.Id);
            ClearCache();
            SetModelBinder(ref model);
            return Ok(result);
        }

        [Route("InsertItem")]
        public IActionResult InsertItem(ShipmentData model)
        {
            ModelState.Clear();
            ModelBinder(ref model);
            SetModelBinder(ref model);
            model.SelectedItem = new();
            return PartialView("Insert", model);
        }

        [Route("DetailItem")]
        public async Task<IActionResult> DetailItem(ShipmentData model)
        {
            ModelBinder(ref model);
            model = await PrepareModel(model);
            model.SelectedItem = model.List.Find(i => i.GetKey() == model.SelectedKey);
            SetModelBinder(ref model);
            return PartialView("Detail", model);
        }

        [Route("UpdateItem")]
        [HttpPost]
        public async Task<IActionResult> UpdateItem(ShipmentData model)
        {
            ModelState.Clear();
            ModelBinder(ref model);
            model = await PrepareModel(model);
            model.SelectedItem = model.List.Find(i => i.GetKey() == model.SelectedKey);
            SetModelBinder(ref model);
            return PartialView("Update", model);
        }

        [Route("DeleteItem")]
        [HttpPost]
        public async Task<IActionResult> DeleteItem(ShipmentData model)
        {
            ModelBinder(ref model);
            model = await PrepareModel(model);
            model.SelectedItem = model.List.Find(i => i.GetKey() == model.SelectedKey);
            SetModelBinder(ref model);
            return PartialView("Delete", model);
        }

        private async Task<ShipmentData> PrepareModel()
        {
            var model = GetModel<ShipmentData>();
            model.CurrentPage = 1;
            model.PageSize = PageSize;
            return await PrepareModel(model);
        }

        private async Task<ShipmentData> PrepareModel(ShipmentData model)
        {
            model.PageSize = ResolvePageSize(model.PageSize);
            if (model.CurrentPage < 1)
                model.CurrentPage = 1;
            var result = await pageClient.ShipmentsListAsync(model.CurrentPage, model.PageSize, includeCount: true);
            model.NumberOfRecord = result?.TotalCount ?? 0;
            model.TotalPage = model.PageSize <= 0 ? 0 : (model.NumberOfRecord + model.PageSize - 1) / model.PageSize;
            if (model.TotalPage > 0 && model.CurrentPage > model.TotalPage)
            {
                model.CurrentPage = model.TotalPage;
                result = await pageClient.ShipmentsListAsync(model.CurrentPage, model.PageSize, includeCount: false);
            }

            model.List = result?.Items?.ToList() ?? new();
            return model;
        }

        private async Task<Dictionary<string, Dictionary<string, string>>> FillPageComboBoxes(Dictionary<string, Dictionary<string, string>> comboBoxList)
        {
            comboBoxList.Clear();
            comboBoxList.AddRange(await FillComboBoxAsync("Orders", async () => ToComboItems((await pageOrderClient.OrdersListAsync())?.Items)));
            comboBoxList.AddRange(await FillComboBoxAsync("ShippingMethods", async () => ToComboItems((await pageShippingMethodClient.ShippingMethodsListAsync())?.Items)));
            return comboBoxList;
        }

        [Route("Queries/GetByOrderId")]
        public async Task<IActionResult> QueryGetByOrderId()
        {
            var model = GetModel<ShipmentGetByOrderIdData>();
            model.CurrentPage = 1;
            model.PageSize = PageSize;
            model.HasRun = false;
            model.List = new();
            model.ComboList = await FillPageComboBoxes(model.ComboList);
            return View("GetByOrderId", model);
        }

        [Route("Queries/GetByOrderId")]
        [HttpPost]
        public async Task<IActionResult> QueryGetByOrderId(ShipmentGetByOrderIdData model)
        {
            model = await PrepareQueryGetByOrderIdModel(model);
            return View("GetByOrderId", model);
        }

        [Route("Queries/GetByOrderIdPartial")]
        [HttpPost]
        public async Task<IActionResult> QueryGetByOrderIdPartial(ShipmentGetByOrderIdData model)
        {
            model = await PrepareQueryGetByOrderIdModel(model);
            return PartialView("_GetByOrderIdTable", model);
        }

        private async Task<ShipmentGetByOrderIdData> PrepareQueryGetByOrderIdModel(ShipmentGetByOrderIdData model)
        {
            model.PageSize = ResolvePageSize(model.PageSize);
            if (model.CurrentPage < 1)
                model.CurrentPage = 1;
            model.HasRun = true;
            model.ErrorMessage = null;
            model.QueryHttpStatus = null;
            model.ComboList = await FillPageComboBoxes(model.ComboList);
            try
            {
                var result = await pageClient.ShipmentsGetByOrderIdAsync(model.ShipmentOrderId, pageNumber: model.CurrentPage, pageSize: model.PageSize);
                model.List = result?.Cast<dynamic>().ToList() ?? new();
                var hasMore = model.List.Count >= model.PageSize;
                var seen = ((model.CurrentPage - 1) * model.PageSize) + model.List.Count;
                model.NumberOfRecord = seen;
                model.TotalPage = hasMore ? model.CurrentPage + 1 : model.CurrentPage;
                model.HasMoreRecords = hasMore;
            }
            catch (QuickCodeSwaggerException ex)
            {
                model.List = new();
                model.NumberOfRecord = 0;
                model.TotalPage = 1;
                model.HasMoreRecords = false;
                model.QueryHttpStatus = ex.StatusCode;
                model.ErrorMessage = FormatPortalQueryError(ex);
            }

            return model;
        }

        [Route("Queries/GetByTrackingNumber")]
        public async Task<IActionResult> QueryGetByTrackingNumber()
        {
            var model = GetModel<ShipmentGetByTrackingNumberData>();
            model.CurrentPage = 1;
            model.PageSize = PageSize;
            model.HasRun = false;
            model.List = new();
            model.ComboList = await FillPageComboBoxes(model.ComboList);
            return View("GetByTrackingNumber", model);
        }

        [Route("Queries/GetByTrackingNumber")]
        [HttpPost]
        public async Task<IActionResult> QueryGetByTrackingNumber(ShipmentGetByTrackingNumberData model)
        {
            model = await PrepareQueryGetByTrackingNumberModel(model);
            return View("GetByTrackingNumber", model);
        }

        [Route("Queries/GetByTrackingNumberPartial")]
        [HttpPost]
        public async Task<IActionResult> QueryGetByTrackingNumberPartial(ShipmentGetByTrackingNumberData model)
        {
            model = await PrepareQueryGetByTrackingNumberModel(model);
            return PartialView("_GetByTrackingNumberTable", model);
        }

        private async Task<ShipmentGetByTrackingNumberData> PrepareQueryGetByTrackingNumberModel(ShipmentGetByTrackingNumberData model)
        {
            model.PageSize = ResolvePageSize(model.PageSize);
            if (model.CurrentPage < 1)
                model.CurrentPage = 1;
            model.HasRun = true;
            model.ErrorMessage = null;
            model.QueryHttpStatus = null;
            model.ComboList = await FillPageComboBoxes(model.ComboList);
            try
            {
                var result = await pageClient.ShipmentsGetByTrackingNumberAsync(model.ShipmentTrackingNumber);
                model.List = result == null ? new() : new List<dynamic>
                {
                    result
                };
                model.NumberOfRecord = model.List.Count;
                model.TotalPage = 1;
                model.CurrentPage = 1;
                model.HasMoreRecords = false;
            }
            catch (QuickCodeSwaggerException ex)
            {
                model.List = new();
                model.NumberOfRecord = 0;
                model.TotalPage = 1;
                model.HasMoreRecords = false;
                model.QueryHttpStatus = ex.StatusCode;
                model.ErrorMessage = FormatPortalQueryError(ex);
            }

            return model;
        }
    }
}